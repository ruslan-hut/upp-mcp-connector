# Выгрузка конфигурации 1С хранится побайтово: Конфигуратор пишет CRLF и UTF-8 с BOM,
# и нормализация переводов строк ломает обратный импорт. -text отключает любую
# конвертацию, включая autocrlf на машине клонирующего.
* -text
